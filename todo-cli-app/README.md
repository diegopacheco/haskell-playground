### Compile
```bash
cd app/
ghci --make Main.hs
```

### Run
```bash
./Main view todo.txt
./Main add todo.txt "Pick up children from drycleaners"
./Main add todo.txt "Pick up children from drycleaners"
./Main remove todo.txt 2
```